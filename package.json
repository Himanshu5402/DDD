{
  "name": "itsybizzz-command-center",
  "version": "0.1.0",
  "private": true,
  "description": "ITSYBIZZ Command Center — AI-powered MERN Business Operating System",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm ci --prefix server --omit=dev && npm ci --prefix client && npm run build --prefix client",
    "start": "npm start --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "seed:pepsi": "npm run seed:pepsi --prefix server",
    "seed:employees": "npm run seed:employees --prefix server",
    "smoke": "npm run smoke --prefix server && npm run smoke:tasks --prefix server && npm run smoke:wave1 --prefix server"
  },
  "author": "ITSYBIZZ",
  "license": "UNLICENSED"
}
